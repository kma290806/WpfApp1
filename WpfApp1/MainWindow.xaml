<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock x:Name="ScoreText" Text="Очки: 0" FontSize="16" Margin="10" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="КЛИКЕР" FontSize="18" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right">
                <TextBlock x:Name="TimerText" Text="Время: 60" FontSize="16" Margin="10" />
            </StackPanel>
        </Grid>
        <Canvas x:Name="GameCanvas" Grid.Row="1" Background="lightgreen" Height="300" Width="780">
        </Canvas>
        <ListBox Grid.Row="2" Margin="13,6,316,5" Name="message"/>
        <Button Grid.Row="2" Name="Start" Content="Start" Height="30" Margin="540,20,29,20" Click="StartButton_Click" />
    </Grid>
</Window>
